# Herdr skill stays official

Install: `npx skills add herdrdev/herdr --skill herdr -g` (all agent types). Upgrade with the same command. **Do not fork** `SKILL.md`.

Scars that are not in upstream (tab titles, sticky `done`, live `agent get`, no duplicate start, same `herdr` on `PATH`) live in [`skills/agent-behavior/SKILL.md`](../skills/agent-behavior/SKILL.md) § Herdr extras. Occupants get them when onboarded (herdr + agent-behavior + conduct). Receipts/EMAIL stay in `conduct`.
